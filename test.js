const now = new Date();

console.log(
    `${now.getHours()}:${now.getMinutes()} ${now.getDate()}-${now.getMonth()}-${now.getFullYear()}`
);
// () => {} -----> doesn't return immedietly. It just creates the function and whenever its be
// being called it returns the value.

// () => () -----> it not just immedietly returns it also it excutes the function.

// Create a list of data (if its an object that will be helpful)
// Make a map function which will genarate a new task component using the () => () syntax and it will use the info from
// the list created before
//
